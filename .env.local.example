# Supabase の接続情報（Settings → API で取得）
# このファイルを .env.local にコピーして実際の値を入れてください。
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=（anon または publishable key）

# --- Web Push（リマインドのプッシュ通知）---
# service_role キー（Settings → API）。サーバー専用の秘密鍵。絶対に公開しない。
SUPABASE_SERVICE_ROLE_KEY=（service_role key）

# VAPID 鍵（`npx web-push generate-vapid-keys` で生成）
NEXT_PUBLIC_VAPID_PUBLIC_KEY=（public key）
VAPID_PRIVATE_KEY=（private key）
VAPID_SUBJECT=mailto:you@example.com

# cron 用の共有シークレット（送信APIの保護。任意の長いランダム文字列）
CRON_SECRET=（ランダムな長い文字列）

# =============================================================
# 店舗別カスタマイズ設定（lib/config.ts）
#   すべて任意。未設定なら既定値で動作する。
#   方式B（1デプロイ=1店舗）では、各オーナーのデプロイでここを設定して切り替える。
#   ※ NEXT_PUBLIC_ はビルド時に埋め込まれるため、変更したら再デプロイが必要。
# =============================================================
# アプリ名（PWAのホーム画面名・ブラウザのタイトル）。既定: シフト管理
NEXT_PUBLIC_BRAND_NAME=
# ログイン画面の見出しに出す店名。既定: 店舗名
NEXT_PUBLIC_STORE_NAME=
# 営業時間（CLOSE は翌日扱い）。既定: 20:00 / 03:00
NEXT_PUBLIC_OPEN_TIME=20:00
NEXT_PUBLIC_CLOSE_TIME=03:00
# 時間選択の刻み（分）。既定: 30
NEXT_PUBLIC_SLOT_MINUTES=30
# 定番シフト（ワンタップ用）をJSON配列で上書き。未設定なら既定の5種。
#   例: [{"label":"通し","start":"19:00","end":"02:00"},{"label":"前半","start":"19:00","end":"23:00"}]
NEXT_PUBLIC_SHIFT_PRESETS=
